<h1>Categories</h1>
<ul>
  <% @categories.each do |category| %>
  <li>
    <%=link_to category.name, category_path(category[:id]) %> - <%= link_to "View Products", category_products_path(category[:id]) %>
  </li>
  <% end %>
</ul>
